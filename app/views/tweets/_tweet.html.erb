<div class="row">
  <div class="col-md-3">
    <h3>
      <%= link_to tweet.user.nickname, tweet_path(tweet.id) %>
      <small> Created: <%= time_ago_in_words(tweet.created_at) %> </small>
    </h3>

  </div>
  <div class="general_formatting">
    <div class="col-md-2">
      <%= tweet.user.full_name %>
    </div>
    <div class="col-md-5">
      <%= tweet.message%>
    </div>
    <div class="col-md-2">
      <%= link_to "Delete Post", tweet_path(tweet.id), method: :delete, class: 'btn btn-sm btn-danger' %>
    </div>
  </div>
</div>
